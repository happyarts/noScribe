# Install with `pip install -r requirements_macOS_arm64.txt`
# Tested stack (2026-07, macOS arm64): torch 2.13 / torchaudio 2.11 /
# torchcodec 0.15 runs pyannote 4 diarization with identical output and speed
# as torch 2.8. noScribe supplies pyannote with an in-memory waveform, so its
# audio path does not require TorchCodec's optional system FFmpeg backend.
torch==2.13
# pyannote.audio's dependency (torch-audiomentations too); noScribe's own
# forced alignment (noScribe/ctc_align.py) is numpy and does not use it.
torchaudio==2.11
torchcodec==0.15.0
av
AdvancedHTMLParser
appdirs
customtkinter
CTkToolTip
faster-whisper
importlib-resources>=5.10; python_version < "3.12"
# >=3.11 honours MPL_IGNORE_SYSTEM_FONTS, which the diarization worker sets;
# see the comment in noScribe/pyannote_mp_worker.py.
matplotlib>=3.11
Pillow
pyannote.audio>=4
# 6.21 or newer: before 6.14 a frozen transformers cannot import itself (its
# import-structure scan fails on __init__.pyc), and before 6.21 a frozen scipy 1.18
# misses scipy._cyutility and array_api_compat's numpy.fft. The Windows spec collects
# all of scipy itself, so its pin to 6.14.1 is enough there.
pyinstaller>=6.21
pyobjc
python-i18n
PyYAML
soundfile
